#!/usr/bin/env Rscript

library(data.table)
library("RColorBrewer")


pdf("ALL_PCA.pdf")


args = commandArgs(trailingOnly=TRUE)

pca <- fread(args[1])
# pca <- fread("chicken.eigenvec")


plot(pca$V3, pca$V4, pch=19, cex=0.4, xlab="", ylab="")
title(xlab="PC1", ylab="PC2")


# text(pca$V3, pca$V4, pca$V2, cex=0.2, pos = 3) ## Do if you want to add sample names

dev.off()